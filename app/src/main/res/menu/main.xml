<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/search"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_search_menu_24dp"
        android:title="@string/search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>

    <item android:id="@+id/settings_menu"
        android:icon="@drawable/ic_settings_menu_24dp"
        android:title="@string/settings" />

    <item android:id="@+id/about_menu"
        android:icon="@drawable/ic_info_menu_24dp"
        android:title="@string/about" />

    <item android:id="@+id/shutdown_app_menu"
        android:icon="@drawable/ic_power_settings_new_menu_24dp"
        android:title="@string/shutdown" />
</menu>